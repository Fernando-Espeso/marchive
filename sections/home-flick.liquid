
{%- for block in section.blocks -%}
    {% case block.type %}
        {% when 'image' %}
             <div class="thumbnail-randmon">
               <img alt="{{ block.settings.single.alt}}" src="{{ block.settings.single | img_url: 'master' }}" />
             </div>
    {% endcase %}
{% endfor %}

{% schema %}
{
  "name": "flick",
  "tag": "div",
  "class": "flick",
  "blocks": [
    {
      "type": "image",
      "name": "image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "item"
        }
      ]
    }
  ]
}
{% endschema %}
